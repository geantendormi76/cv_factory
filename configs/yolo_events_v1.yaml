# 文件: configs/yolo_events_v1.yaml
# 职责: [最终的单一事实来源] 定义本次任务的所有配置。
#       服务于 prepare_data.py (读取 names) 和 train.py (读取所有参数)。

# --- 1. 核心训练设置 ---
# 【核心起点】从一个预训练模型开始
model: yolov8n.pt  # 或者 /home/zhz/deepl/models/yolov8n.pt

# 【核心数据】指向数据准备脚本将要生成的 dataset.yaml
# 请使用绝对路径以保证健壮性
data: /home/zhz/deepl/data/processed/yolo_events_v1_dataset/dataset.yaml

# 【核心训练参数】
epochs: 200
patience: 30
batch: 8
imgsz: 640
device: 0
workers: 8
project: /home/zhz/deepl/runs/
name: yolo_events_v1_run
exist_ok: True

# --- 2. 优化器与学习率 ---
optimizer: auto
lr0: 0.01
lrf: 0.01

# --- 3. 类别名称 (供 prepare_data.py 使用) ---
# 【核心】这个列表是数据转换的依据，必须与修复后的 .json 标签一致。
names:
  0: 伤害数值
  1: 恢复数值